{
  "type": "postgres",
  "host": "localhost",
  "port": 5000,
  "username": "postgres",
  "password": "password",
  "database": "libquality",
  "entities": [
    "./src/models/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations/"
  }
}
